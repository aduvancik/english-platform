const AUTH_ROUTES = {
    login: "/auth/login",
    register: "/auth/register"
}

const GROUP_ROUTES = {
    base: "/study-groups",
    generate: "/study-groups/generate",
}

export const API_ROUTES = {
    auth: AUTH_ROUTES,
    groups: GROUP_ROUTES,
    teachers: "/teachers",
    students: "/students",
}